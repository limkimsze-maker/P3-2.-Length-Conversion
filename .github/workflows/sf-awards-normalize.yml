name: Normalize Sports Fiesta awards
permissions:
  contents: write
on:
  push:
    branches: [main]
jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install medal-only award hook
        run: |
          python - <<'PY'
          from pathlib import Path
          import re
          p=Path('index.html')
          s=p.read_text(encoding='utf-8')
          s=re.sub(r'\s*<script[^>]+src=["\'][^"\']*sports-fiesta-(?:medal-hook-v4|awards-v1)\.js[^"\']*["\'][^>]*></script>\s*','\n',s,flags=re.I)
          tag='<script src="https://limkimsze-maker.github.io/P3-Length-Mass-and-Volume-Sports-Fiesta-/sports-fiesta-awards-v1.js?v=20260819b" data-practice="2" data-sport="Javelin Throw"></script>'
          i=s.lower().rfind('</body>')
          if i<0: raise SystemExit('No </body> found')
          s=s[:i]+tag+'\n'+s[i:]
          p.write_text(s,encoding='utf-8')
          PY
      - name: Commit award hook only
        run: |
          if git diff --quiet -- index.html; then exit 0; fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add index.html
          git commit -m "Use medal-only Sports Fiesta award hook"
          git push
